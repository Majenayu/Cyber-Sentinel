kind = "web"
previewPath = "/"
title = "CyberSentinel"
version = "1.0.0"
id = "artifacts/cyber-sentinel"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25629

[services.development]
run = "pnpm --filter @workspace/cyber-sentinel run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cyber-sentinel", "run", "build" ]
publicDir = "artifacts/cyber-sentinel/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25629"
BASE_PATH = "/"
